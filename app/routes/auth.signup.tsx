/*
  This is a sub-route of Auth and will render inside
  the Auth <Outlet />
*/

export default function SignUp() {
  return <div>SignUp</div>;
}

export const action = async () => {
  // This function will be for submitting the SignUp data
  // to the API
};
