/*
the boxes for layouts on logged-in pages
 */

import React from "react";

type DashboardProps = {
    variant: "placeholder"
}

function DashboardModule({variant}: DashboardProps) {
    return(
        <div>
            .
        </div>
    )
}

export default DashboardModule
