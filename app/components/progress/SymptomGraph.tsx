import React from "react";

type SymptomGraphProps = {
    // TODO
}

function SymptomGraph() {
    return(
        <div>
            Symptom graph (Maxine todo - dont worry about symptom selecting or dropdown rn, just the graph itself)
        </div>
    )
}

export default SymptomGraph
